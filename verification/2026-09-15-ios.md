# IOM iOS CLI verification - 2026-09-15

- CLI integration base: upstream 2.3.0 `93364a2455c2`, merge `1c58436`.
- Input: IOM commit `ec67654ea` archived from the clean game integration worktree.
- Runtime: exact stock `2024.14.4.268`, YYC.
- Host: Mac worker, Node 24.18.0, Xcode 26.1.1 (17B100).
- Dedicated source, CLI and caches under `/Users/jd/gm-cli-ios-validation-20260915`; existing IDE projects and caches were not modified.

## Passed

1. `package --target ios --runtime native --toolchain GMS2@2024.14.4.268`: generated one fresh Xcode project, prepared extension XCFramework ZIPs, returned its exact path and both runner hashes. No IDE devices file was supplied.
2. `compile` with the same project/runtime: generated a separate project, ran CocoaPods and a complete unsigned Xcode Release device build. Xcode reported `BUILD SUCCEEDED`; the CLI verified the staged runner and wrote `compile-v2-result.json`.
3. Device library SHA-256: `51e8385ae9bad686ab5977af3915fd7b9cc0d2dea68b2ce59be8b058fc7fc6a9`.
4. Simulator library SHA-256: `15cacb0286f4a69338c3ccc27229f3b1c7250ea915c856da9487e57b2214f84d`.
5. `run` built IOM for a newly created iOS 26.1 iPhone 17 Pro simulator, verified the simulator runner, booted it, installed the app and launched it successfully. The current candidate removed its temporary user/settings directory after completion.
6. Local mobile unit tests on Node 24.18.0, TypeScript build and lint checks.

The worker's `/opt/homebrew/bin/pod` failed with an ffi dependency conflict. Its existing `/usr/local/bin/pod` (1.16.2) succeeded with UTF-8 locale; validation used that PATH ordering without changing global gems.

## Still to verify

Direct CLI IPA export with an unlocked signing identity, VM simulator run and physical-device behavior. The CLI-generated project has passed signed IPA export through Jenkins/Fastlane (below). Subsequent custom runner consumption and the actual Discord-triggered upload are recorded in [custom mobile acceptance](2026-09-16-custom-mobile.md).

The candidate now uses Igor bundled with the exact official mobile runtime. A live test found that this Igor rejects upstream's newer `-jsonErrors` argument; mobile invocations now omit that option. The matching-Igor candidate generated IOM and reached a complete Xcode archive build. Manual signing is scoped to the app target, leaving Pods unaffected. Signing then failed with `errSecInternalComponent` while using the correct Checkbox distribution identity; no successful IPA/export record was emitted. Jenkins uses a fresh temporary keychain with the existing match credentials to avoid dependence on the login keychain's interactive access state.

The final local suite contains 14 TypeScript and 11 Python tests, including verified assembly, independent caches, wrong base hashes, artifact tampering and the retired signing-wrapper rejection. Native compilation is not evidence of on-device acceptance. The device compile used an earlier candidate; the simulator run verified the subsequent cleanup changes.

## Published CLI and pipeline verification

The published artifact is `checkbox-2.3.0-mobile.1`, source `949bda0fe0fa90e9810cf98b3b30650a78b94c40`, tarball SHA-256 `b41a30d98d8fcc9626321479f5c82efde7c28bef8502f236978e636aae39214f`. Both workers have the same artifact and Node 24.18.0 installed. The published artifact also generated the real IOM VM Xcode project successfully (`vm-pinned-result.json`); A subsequent full VM device compile passed as recorded below.

Jenkins `IOM_Deploy/PR-135/1`, game source `d438b651c11dbcce1791557f863f4ac1adefa7e0`, verified stock `2024.14.4.268` Android packaging and signing through the upstream CLI:

- AAB SHA-256: `2b8845bdbbf72cc1d4c2a17084e67593cf63cc6010c7a23f1adcc191dd4dcb02`.
- Signer certificate SHA-256: `8b9a54be9cfb3619fbf4affeb187146a81b5ea602efa2ba0e0c971b01a817313`.
- Native architectures: `arm64-v8a`, `x86_64` only.
- AAB and provenance archived; no Play upload requested.
- Overall build failed in its separate iOS branch because Jenkins supplied a malformed `GEM_HOME`. The pipeline now clears inherited Ruby environment overrides and uses the verified CocoaPods installation. The separate iOS-only build #2 passed signing, archive/export and runner/symbol verification (below).

These are Jenkins API tests, not Discord acceptance. The custom native packages from `IOM_Runner/4` passed manifest, archive-header and architecture verification by the published CLI consumer. `IOM_Runner_Releases/3` failed publication with HTTP 403 because the Jenkins GitHub App has read-only Contents permission. That publication blocker was subsequently resolved as recorded below; custom game acceptance remains separate.

## Signed IOM iOS package

Jenkins `IOM_Deploy/PR-135/2` generated the project through the pinned CLI and completed Fastlane signing/archive/export. Input game source: `2f0887ca954143f21b106c5daf2ee321fda10de0`; exact stock runtime `2024.14.4.268`. Marketing version `2.2.21`, app-wide reserved build number `101`.

- IPA SHA-256: `860ca66e469d88f7f74a03325ced876a5786be2690b8e593c177a0ab952face9`.
- dSYM ZIP SHA-256: `ba9af668b889e1f6a562c54d9bc7e0734dbfd8a8247422d1bc247281ff57841c`.
- Matching app and dSYM UUID: `C3D32A15-1620-3A9A-9965-FC577B6E3AC3` (`arm64`).
- Staged runner SHA-256: `51e8385ae9bad686ab5977af3915fd7b9cc0d2dea68b2ce59be8b058fc7fc6a9`.
- Temporary signing keychain removed after export; no login-keychain changes required.
- Jenkins build #2 finished SUCCESS, including archival and workspace cleanup. The archived IPA and canonical dSYM ZIP were downloaded after completion and both SHA-256 values matched the verified provenance. A separately rehashed local copy is preserved in the numbered validation record.
- No TestFlight or App Store upload selected. This verifies the stock fallback and shared iOS build path, not custom runtime/device acceptance.

## Final additional checks

The published CLI completed `compile --target ios --runtime vm --toolchain GMS2@2024.14.4.268` against the same isolated IOM source. Xcode reported `BUILD SUCCEEDED`; `vm-compile-pinned-result.json` records the fresh project/DerivedData paths and verified staged VM device runner SHA-256 `40ad8b06ce834ecefb4cd85f4ec81176bf7036b80f83767ac874de304716ab4a` (simulator library inventory SHA-256 `e2c64518f3eca6c98bb898ddabd6bc4706f3baa8aa25b02cbf7dfa3834c981ab`). This was an unsigned device compile, not VM simulator/device play acceptance.

The exported YYC IPA also passed independent `codesign --verify --deep --strict` verification after extraction. Its identity is `Apple Distribution: Checkbox Entertainment LTD (87MP2AWGBS)`, team `87MP2AWGBS`, bundle `com.checkbox.idleobeliskminer`.

## Published c1 runtime

`IOM_Runner_Releases/5` subsequently finished SUCCESS and published immutable release `389506427` (`runner/2024.14.4.268-c1`) from the validated build #4 packages. The pinned game CLI resolved both packages from GitHub and passed asset digest, full inventory, native architecture and XCFramework metadata checks. Manifest SHA-256: `89fe80d83df9b7c1b11eb862822a91c872da632e4d7693492c644bff6eeae0af`; runner source `e842ab09f758cf2d2fe441ddb6437111d22e526c`. GitHub's immutable-release attestation also verified successfully.

The game changes are now directly pushed to `ci/runner-releases` (`c155290051c414fcb17aca3af5d50715d952540f`). Both temporary PRs are closed and their remote branches removed. The original stock validation jobs' artifacts and metadata were preserved under the numbered local validation records before that cleanup. Subsequent custom game/TestFlight evidence and remaining device checks are recorded in [custom mobile acceptance](2026-09-16-custom-mobile.md).
