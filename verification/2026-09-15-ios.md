# IOM iOS CLI verification - 2026-09-15

- CLI integration base: upstream 2.3.0 `93364a2455c2`, merge `1c58436`.
- Input: IOM commit `ec67654ea` archived from the clean game integration worktree.
- Runtime: exact stock `2024.14.4.268`, YYC.
- Host: Mac worker, Node 24.18.0, Xcode 26.1.1 (17B100).
- Dedicated source, CLI and caches under `/Users/jd/gm-cli-ios-validation-20260915`; existing IDE projects and caches were not modified.

## Passed

1. `package --target ios --runtime native --toolchain GMS2@2024.14.4.268`: generated one fresh Xcode project, prepared extension XCFramework ZIPs, returned its exact path and both runner hashes. No IDE devices file was supplied.
2. `compile` with the same project/runtime: generated a separate project, ran CocoaPods and a complete unsigned Xcode Release device build. Xcode reported `BUILD SUCCEEDED`; the CLI verified the staged runner and wrote `compile-v2-result.json`.
3. Device library SHA-256: `51e8385ae9bad686ab5977af3915fd7b9cc0d2dea68b2ce59be8b058fc7fc6a9`.
4. Simulator library SHA-256: `15cacb0286f4a69338c3ccc27229f3b1c7250ea915c856da9487e57b2214f84d`.
5. `run` built IOM for a newly created iOS 26.1 iPhone 17 Pro simulator, verified the simulator runner, booted it, installed the app and launched it successfully. The current candidate removed its temporary user/settings directory after completion.
6. Local mobile unit tests on Node 24.18.0, TypeScript build and lint checks.

The worker's `/opt/homebrew/bin/pod` failed with an ffi dependency conflict. Its existing `/usr/local/bin/pod` (1.16.2) succeeded with UTF-8 locale; validation used that PATH ordering without changing global gems.

## Still to verify

Signed IPA export, VM builds, custom runner publication/consumption, Discord-triggered TestFlight upload and physical-device behavior.

The candidate now uses Igor bundled with the exact official mobile runtime. A live test found that this Igor rejects upstream's newer `-jsonErrors` argument; mobile invocations now omit that option. The matching-Igor candidate generated IOM and reached a complete Xcode archive build. Manual signing is scoped to the app target, leaving Pods unaffected. Signing then failed with `errSecInternalComponent` while using the correct Checkbox distribution identity; no successful IPA/export record was emitted. Jenkins uses a fresh temporary keychain with the existing match credentials to avoid dependence on the login keychain's interactive access state.

The final local suite contains 14 TypeScript and 11 Python tests, including verified assembly, independent caches, wrong base hashes, artifact tampering and the retired signing-wrapper rejection. Native compilation is not evidence of on-device acceptance. The device compile used an earlier candidate; the simulator run verified the subsequent cleanup changes.
